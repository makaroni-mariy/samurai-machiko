<%# blackの場合は背景が黒、文字色が白 %>
<% if( backColor == 'black') { %>
  <a href="<%= link %>" class="c-btn--black"><%= text %></a>
<%# halfの場合はSP版は背景が黒、PC版は背景が白 %>
<% } else (backColor == 'half') { %>
  <a href="<%= link %>" class="c-btn--half"><%= text %></a>
<% } %>




